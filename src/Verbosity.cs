namespace APSIM.Bootstrapper.Logging
{
    /// <summary>
    /// Describes a message type.
    /// </summary>
    public enum Verbosity
    {
        Diagnostic,
        Information,
        Warning,
        Error
    }
}